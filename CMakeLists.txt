# Copyright MediaZ Teknoloji A.S. All Rights Reserved.

cmake_minimum_required(VERSION 3.24.2)
project("NOSAIModules")

set_property(GLOBAL PROPERTY USE_FOLDERS ON)
set(CMAKE_CXX_STANDARD 20)

macro(get_targets targets dir)
    get_property(subdirectories DIRECTORY ${dir} PROPERTY SUBDIRECTORIES)
    foreach(subdir ${subdirectories})
        get_targets(${targets} ${subdir})
    endforeach()
    get_property(current_targets DIRECTORY ${dir} PROPERTY BUILDSYSTEM_TARGETS)
    foreach(subtarget ${current_targets}) 
        if(TARGET ${subtarget})
            list(APPEND ${targets} ${subtarget})
        endif()
    endforeach()
endmacro()

macro(group_targets targets folder_name)
    foreach(target ${targets})
        get_target_property(FOLD ${target} FOLDER)
        if(${FOLD} STREQUAL "FOLD-NOTFOUND")
            set(FOLD_NAME "${folder_name}")
        else()
            set(FOLD_NAME "${folder_name}/${FOLD}")
        endif()
        set_target_properties(${target} PROPERTIES FOLDER ${FOLD_NAME})
    endforeach()
endmacro()

add_compile_definitions(NOS_DISABLE_DEPRECATED)
nos_find_sdk("1.3.0" NOS_PLUGIN_SDK_TARGET NOS_SUBSYSTEM_SDK_TARGET NOS_SDK_DIR)
nos_get_module("nos.sys.vulkan" "6.21" NOS_SYS_VULKAN_TARGET)

# Check for environment variable & cmake variable
if(NOT DEFINED CUDA_SDK_PATH)
	if(NOT DEFINED ENV{CUDA_PATH})
		message(FATAL_ERROR "CUDA_PATH environment or cmake variable is not set.")
	endif()
	set(CUDA_SDK_PATH $ENV{CUDA_PATH})
endif()
message(STATUS "CUDA_PATH: ${CUDA_SDK_PATH}")
if(NOT DEFINED CUDNN_SDK_PATH)
	if(NOT DEFINED ENV{CUDNN_SDK_PATH})
		message(FATAL_ERROR "CUDNN_SDK_PATH environment variable is not set.")
	endif()
	set(CUDNN_SDK_PATH $ENV{CUDNN_SDK_PATH})
endif()
message(STATUS "CUDNN_SDK_PATH: ${CUDNN_SDK_PATH}")
if(NOT DEFINED TENSORRT_SDK_PATH)
	if(NOT DEFINED ENV{TENSORRT_SDK_PATH})
		message(FATAL_ERROR "TENSORRT_SDK_PATH environment variable is not set.")
	endif()
	set(TENSORRT_SDK_PATH $ENV{TENSORRT_SDK_PATH})
endif()
message(STATUS "TENSORRT_SDK_PATH: ${TENSORRT_SDK_PATH}")
if(NOT DEFINED ONNX_SDK_PATH)
	if(NOT DEFINED ENV{ONNX_SDK_PATH})
		message(FATAL_ERROR "ONNX_SDK_PATH environment variable is not set.")
	endif()
	set(ONNX_SDK_PATH $ENV{ONNX_SDK_PATH})
endif()
message(STATUS "ONNX_SDK_PATH: ${ONNX_SDK_PATH}")

set(TOOLS_DIR ${CMAKE_CURRENT_SOURCE_DIR}/Tools/Windows)
set(FLATC_EXECUTABLE ${NOS_SDK_DIR}/bin/flatc)

set(SUBSYSTEMS_SOURCE_DIR ${CMAKE_CURRENT_SOURCE_DIR}/Subsystems)
add_subdirectory("Subsystems" "${CMAKE_CURRENT_BINARY_DIR}/Subsystems")
get_targets(SUBSYSTEM_TARGETS "${CMAKE_CURRENT_BINARY_DIR}/Subsystems")
group_targets("${SUBSYSTEM_TARGETS}" "NOS AI Subsystems")

add_subdirectory("Plugins" "${CMAKE_CURRENT_BINARY_DIR}/Plugins")
get_targets(PLUGIN_TARGETS "${CMAKE_CURRENT_BINARY_DIR}/Plugins")
group_targets("${PLUGIN_TARGETS}" "NOS AI Plugins")